#!/usr/bin/env direnv

# Add local bin directory to PATH
export PATH="$(pwd)/bin:$PATH"

# Check if td binary is built and show appropriate message
if [ -f "$(pwd)/bin/td" ]; then
    echo "Using locally built td version 🚀"
else
    echo "Local td version not built. Run 'make build' to build it."
fi
